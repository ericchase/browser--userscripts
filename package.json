{
  "type": "module",
  "scripts": {
    "build": "bun run ./tools/build.ts",
    "dev": "bun run ./tools/watch-build.ts",
    "format-debug": "prettier . --debug-check",
    "format": "prettier . --write"
  },
  "devDependencies": {
    "@types/bun": "^1.1.6",
    "@types/node": "^20.14.9",
    "prettier": "^3.3.2"
  }
}
