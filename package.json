{
  "scripts": {
    "build": "bun ./automation-scripts/generate-links.mts",
    "dev": "tsc --watch",
    "format-debug": "prettier . --debug-check",
    "format": "prettier . --write"
  },
  "dependencies": {
    "prettier": "^3.3.2",
    "typescript": "^5.4.5"
  }
}
