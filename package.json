{
  "scripts": {
    "generate-links": "bun ./automation-scripts/generate-links.mts",
    "format-debug": "prettier . --debug-check",
    "format": "prettier . --write"
  },
  "dependencies": {
    "prettier": "^3.3.2"
  }
}
